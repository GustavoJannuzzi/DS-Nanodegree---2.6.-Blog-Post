# Raw CSV is too large for GitHub (135 MB) — use the .zip in the same folder
stack-overflow-developer-survey-2025/survey_results_public.csv

# Build script (internal tooling, not needed in repo)
build_notebook.py

# Python cache
__pycache__/
*.pyc
*.pyo

# Jupyter checkpoints
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db
